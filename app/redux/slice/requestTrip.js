import { createSlice, createAsyncThunk } from "@reduxjs/toolkit";

const initialState = {
    tripRequest: {},
    isError: false,
    isSuccess: false,
    isLoading: false,
    message: "",
}

export const tripSlice= createSlice({
         
})