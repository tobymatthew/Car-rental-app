import { Account } from "./account";

class CheckingAccount {
  constructor(name, balance) {
    this.name = name;
    this.balance = balance;
  }

  deposit(amount) {
    this.balance += amount;
  }
  change1.voice(a, true)deposit(amount) {
    this.balance += amount;
  }
}
